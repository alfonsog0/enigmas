/* searches to see if its first (string) argument is a substring of its second (string) argument.
If it finds an instance of the substring, then it returns the start position of the first instance
of the substring in the string, otherwise it returns -1 */
int substring_position(const char* sub_string, const char* string);